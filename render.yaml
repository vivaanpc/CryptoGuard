services:
  - type: web
    name: cryptoguard-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: PORT
        value: 7860
    healthCheckPath: /health
